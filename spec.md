# LMS Auto-Watch & Transcript — Spec

> 작성: 2026-03-02 20:42
> 갱신: 2026-03-03
> 상태: 초안 — 리뷰 필요

## 한 줄 목적

숭실대 LMS 사전녹화 강의를 자동 재생(출석 처리)하고, 재생 중 스크립트를 추출해두는 도구.

## 왜 필요한가

- 시간이 없고 귀찮음 — 사전녹화 강의를 직접 앉아서 볼 여유가 없음
- 깜빡 놓치면 성적 깎임 — 마감 전에 출석 처리가 안 되면 감점
- 스크립트가 있으면 나중에 빠르게 복습 가능

## 사용자

나 (숭실대 학부생, 1명). 개인 도구.

## 사용 모드

### 모드 A — 수동 (CLI)

로컬 PC에서 직접 실행. 내가 고른 강의만 재생.

```
$ python -m src.auto_watch

숭실대 LMS 자동 수강
━━━━━━━━━━━━━━━━━━━━

로그인 중... ✓

미수강 강의 3개:
  [1] 자료구조 — chap1_1 (24:22) D-6
  [2] 자료구조 — chap1_2 (31:51) D-6
  [3] 감성지능리더십 — 1주차 (15:30) D-13

재생할 번호 (예: 1,2 / all / q) [10초 후 자동 all]: 1,2

[1/2] chap1_1 재생 중... 24:22
  ├ 출석 처리: 재생 중 (12:11 / 24:22)
  └ 스크립트: 추출 중...
[2/2] chap1_2 재생 중... 31:51
  ├ 출석 처리: 완료 ✓
  └ 스크립트: 저장 완료 → transcripts/자료구조_chap1_2.txt

완료! 2개 수강, 총 56:13
```

**흐름:**
1. 실행 → SSO 자동 로그인
2. 미수강 강의 목록 표시 (과목명, 제목, 재생시간, 마감까지 남은 일수)
3. 사용자가 번호 선택 (복수 선택, `all`, `q`로 종료). 10초간 입력 없으면 자동으로 `all` 선택
4. 선택한 강의를 순차 재생 + 병렬로 스크립트 추출
5. 결과 요약 출력

### 모드 B — 자동 (cron)

OpenClaw + cron으로 주기적 실행. 사용자 개입 없이 전체 자동.

```
$ python -m src.auto_watch --auto
```

**흐름:**
1. cron 트리거 (예: 매주 월요일 새벽)
2. 로그인 → 시작일 지난 미수강 강의 전부 재생
3. 스크립트 추출
4. 텔레그램으로 결과 알림 ("3개 수강 완료, 스크립트 3개 저장")

## 파이프라인

강의 1개당 두 가지 작업이 동시에 진행:

```
강의 재생 시작
  ├─ [출석] 1x 배속 재생 → 완료 이벤트 대기 → 출석 인정
  └─ [스크립트] 영상 다운로드 → faster-whisper로 음성→텍스트 추출
```

- **출석**: 브라우저에서 1x 재생. 서버가 누적 시간 추적하므로 끝까지 재생 필수
- **스크립트 추출**: 영상 파일 다운로드 → faster-whisper (로컬, API 불필요)
- **요약 (optional)**: Gemini 무료 tier 또는 수동으로 Claude에 붙여넣기. 핵심 스코프 아님

### 저장 경로

```
output/
  └─ {과목명}/
      ├─ {강의명}.mp4    # 영상 파일
      └─ {강의명}.txt    # 스크립트 (음성→텍스트)
```

예: `output/자료구조/chap1_1.mp4`, `output/자료구조/chap1_1.txt`

## 성공 기준

- [ ] 모드 A: 선택한 강의만 재생되고, 미선택 강의는 건드리지 않음
- [ ] 모드 A: 재생 완료 후 LMS에서 출석 인정 확인
- [ ] 모드 A: 스크립트 텍스트 파일과 영상 파일이 output/ 에 저장됨
- [ ] 모드 B: `--auto` 플래그로 전체 자동 실행
- [ ] 모드 B: 텔레그램 알림 수신
- [ ] 시작일 전 강의는 재생 대상에서 제외
- [ ] 1x 배속 유지 (서버 배속 감지 통과)

## 제약

- **1x 배속 필수**: LMS 서버가 `invalidPbrate`로 배속 감지. 초과 시 부정행위 처리
- **동시 수강 불가**: 창 여러 개로 동시 재생하면 안 됨. 순차 재생만
- **headed 브라우저**: 헤드리스면 LTI 플레이어가 완료 이벤트 안 보낼 수 있음
- **SSO 로그인**: `page.type` + JS `btn_click` 방식 (Playwright `fill`이 안 먹힘)

## 범위 밖 (Out of Scope)

- LLM 요약 자동화 (스크립트 추출까지만. 요약은 수동)
- 과제/퀴즈 자동 제출
- 실시간 강의 (Zoom 등) 자동 참석
- 모바일 앱

## 기술 스택

- **언어**: Python 3.11+
- **브라우저 자동화**: Playwright (headed, Chromium)
- **음성→텍스트**: faster-whisper (CTranslate2, 로컬)
- **자동화 트리거**: OpenClaw (cron + 텔레그램)
- **기존 코드**: lms-summarizer 레포 (Playwright, faster-whisper 이미 셋업됨)

## 마일스톤

1. **M1 — 수동 선택 재생**: CLI 목록 → 번호 선택 → 재생 → 출석 확인 ✅
2. **M2 — 스크립트 추출 연결**: 재생 중 영상 다운로드 + faster-whisper 파이프라인 연결 ✅
3. **M2.5 — 수강완료 강의 다운로드**: 아래 상세 스펙 참고
4. **M3 — 자동 모드**: `--auto` 플래그 + 전체 자동 재생
5. **M4 — OpenClaw + 텔레그램**: cron 트리거 + 결과 알림

---

# M2.5 — 수강완료 강의 다운로드 지원

## 문제

현재 도구는 **미수강 강의만** 대상으로 동작한다.

| | 자동 수강 처리 | 다운로드 + 전사 |
|---|---|---|
| 미수강 강의 | O | O |
| 수강완료 강의 | 불필요 | **X ← 문제** |

이미 들은 강의의 영상/스크립트를 확보할 수 없다. 두 가지 필터가 원인:

1. **과목 필터** (`courses.py:46`): `videoCount > 0`인 과목만 → 전부 수강 완료된 과목은 안 뜸
2. **강의 필터** (`courses.py:132`): `isCompleted === true`인 강의 제외

## 해결 방향

**모든 강의를 보여주되, 상태에 따라 동작을 분기한다.**

- **미수강** → 기존대로: 자동 재생 + 완료 대기 + 다운로드 + 전사
- **수강완료** → 재생 버튼 클릭(URL 캡처용) → 즉시 정지 → 다운로드 + 전사만

---

### 1. 과목 조회

**함수명**: `get_unwatched_courses()` → `get_courses()`

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 필터 | `videoCount > 0`만 반환 | 모든 과목 반환 (필터 제거) |
| `videoCount` | 필터링 용도 | UI 표시용으로만 유지 |
| 빈 결과 | "모든 강의 수강 완료" | "수강 중인 과목이 없습니다" |

**로그 출력**:

```
[INFO] 과목 5개 발견 (미수강 동영상 있는 과목: 3개):
  • 컴퓨터네트워크 — 미수강 5개
  • 데이터베이스 — 미수강 3개
  • 운영체제 — 수강 완료
  • 알고리즘 — 수강 완료
  • 소프트웨어공학 — 미수강 1개
```

---

### 2. 강의 조회

**함수명**: `get_unwatched_lectures()` → `get_lectures()`

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| `isCompleted` 필터 | true면 제외 | **제거** — 수강완료도 포함 |
| 시작일 필터 | 오늘 이후 제외 | 유지 (미래 강의는 접근 불가) |
| `isCompleted` 필드 | 생성 후 필터에서 소비 | **보존** — UI 표시 + 분기용 |

**반환 데이터** (변경 없음, `isCompleted` 필드만 보존):

```python
{
    "title": str,
    "href": str,
    "isCompleted": bool,      # 수강완료 여부
    "durationSec": int,
    "startDate": str | None,
    "deadline": str | None,
    "courseName": str
}
```

---

### 3. CLI 선택 UI

**변경 후 UI**:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  전체 강의 15개 (미수강 3 / 수강완료 12):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ── 알고리즘 (미수강 2 / 전체 5) ──
  [ 1]    알고리즘 — 1주차 OT (24:30) D-3
  [ 2]    알고리즘 — 2주차 정렬 (35:12) D-3
  [ 3] V  알고리즘 — 3주차 탐색 (20:00)
  [ 4] V  알고리즘 — 4주차 그래프 (18:45)
  [ 5] V  알고리즘 — 5주차 DP (22:10)

  ── 운영체제 (수강 완료) ──
  [ 6] V  운영체제 — 1주차 프로세스 (30:00)
  [ 7] V  운영체제 — 2주차 스레드 (28:00)
  ...

  총 재생시간: 5:30:00 (미수강: 59:42)

번호 (예: 1,2,3 / all / q) [10초 후 자동 all]:
```

| 항목 | 설명 |
|------|------|
| 헤더 | `전체 강의 N개 (미수강 X / 수강완료 Y)` |
| 과목별 구분 | 과목명 구분선 + 과목별 카운트 |
| 상태 표시 | 미수강: 공백, 수강완료: `V` |
| D-day | 수강완료는 표시 안 함 |
| 재생시간 | 전체 + 미수강만 분리 표시 |
| 정렬 | 과목 내 주차 순서 유지 |
| `all` | 전체 선택 (수강완료 포함) |

---

### 4. 플레이어 분기

**함수명**: `watch_lecture()` → `process_lecture()`

`isCompleted` 여부에 따라 분기:

```
┌─────────────────────────────────────────────┐
│              공통                             │
│  페이지 이동 → iframe 대기 → 이어보기 처리    │
│  → 재생 버튼 클릭 → 비디오 URL 캡처           │
└──────────────┬──────────────────┬────────────┘
               │                  │
         미수강 강의          수강완료 강의
               │                  │
      진행 모니터링 루프      즉시 비디오 정지
      (완료 감지까지 대기)         │
               │                  │
┌──────────────┴──────────────────┴────────────┐
│              공통                             │
│  다운로드 + 전사 → 결과 반환                   │
└─────────────────────────────────────────────┘
```

**수강완료 강의 처리 흐름**:

1. 강의 페이지 진입
2. iframe 대기 + 이어보기 다이얼로그 → "아니오"
3. **재생 버튼 클릭** (비디오 URL 캡처 트리거)
4. URL 캡처 대기 (최대 5초)
5. **즉시 `video.pause()`** — 불필요한 재생 방지
6. 다운로드 + 전사 시작
7. 진행 모니터링 **스킵**
8. 결과 반환

**로그**:

```
미수강:   [PLAY] 2주차 정렬 (35:12)
수강완료: [DL] 3주차 탐색 (수강완료 — 다운로드만)
```

---

### 5. 결과 보고

```
══════════════════════════════════════════
  완료!
  수강 처리: 3개
  다운로드: 5개 (수강완료 강의)
  스크립트: 8개 추출 -> output/
  수강 실패: 1개
══════════════════════════════════════════
```

카운터: `watch_completed` / `download_only` / `transcribed` / `watch_failed`

해당 카운트가 0이면 해당 줄 생략.

---

### 6. 엣지 케이스

| 케이스 | 대응 |
|--------|------|
| 수강완료 강의에서 비디오 URL 캡처 실패 | 기존 로직 유지 — "영상 URL 미감지" 출력, 스킵 |
| 수강완료 강의의 이어보기 다이얼로그 | 기존 로직 유지 — "아니오" 클릭 |
| 시작일이 미래인 강의 | 기존 필터 유지 — 제외 |
| 동영상 강의가 없는 과목 | `durationSec <= 0` 필터로 자연스럽게 제외 |
| 이미 다운로드된 파일 존재 | 현행 유지 (덮어쓰기) |

---

### 7. 수정 대상 파일

| 파일 | 변경 내용 |
|------|----------|
| `src/auto_watch/courses.py` | 필터 제거, 함수 리네이밍, 로그 변경 |
| `src/auto_watch/cli.py` | 상태 표시 UI, 과목별 그룹핑, 재생시간 분리 |
| `src/auto_watch/player.py` | `isCompleted` 분기 + 즉시 정지 로직 |
| `src/auto_watch/main.py` | import 변경, 카운터 분리, 결과 보고 |

### 8. 함수명 변경 요약

| 파일 | 변경 전 | 변경 후 |
|------|---------|---------|
| `courses.py` | `get_unwatched_courses()` | `get_courses()` |
| `courses.py` | `get_unwatched_lectures()` | `get_lectures()` |
| `player.py` | `watch_lecture()` | `process_lecture()` |
| `main.py` | import + 호출부 | 위 변경에 맞춰 수정 |
